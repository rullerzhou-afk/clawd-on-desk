#!/usr/bin/env bash
echo "Clawd: scripts/remote-deploy.sh is disabled because it cannot create a profile-bound secure ingress. Use Settings -> Remote SSH -> Deploy / Repair Hooks in the Clawd app." >&2
exit 2
